<%= form_for(@code_academy) do |f| %>
  <% if @code_academy.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@code_academy.errors.count, "error") %> prohibited this code_academy from being saved:</h2>

      <ul>
      <% @code_academy.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :content %><br>
    <%= f.text_area :content %>
  </div>
  <div class="field">
    <%= f.label :user_id %><br>
    <%= f.number_field :user_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
